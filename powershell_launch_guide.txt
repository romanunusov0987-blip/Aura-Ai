# Гайд по запуску Aura-Ai через PowerShell

Ниже приведена последовательность готовых команд для Windows PowerShell, которая поможет развернуть проект Aura-Ai и запустить все основные компоненты (основной Telegram-бот, административный бот и реферальный backend-сервис) одной командой — через сценарий-оркестратор `run_all.py`.

> 💡 Предполагается, что Git и Python 3.11+ уже установлены и доступны в PATH. PostgreSQL требуется только если вы переводите реферальный сервис с SQLite на внешнюю БД.

## 1. Клонирование репозитория и переход в каталог проекта
```powershell
# Склонировать репозиторий (при необходимости замените URL на свой форк)
git clone https://github.com/your-account/Aura-Ai.git

# Перейти в папку проекта
Set-Location Aura-Ai
```

## 2. Создание и активация виртуального окружения Python
```powershell
# Создать виртуальное окружение в папке .venv
python -m venv .venv

# Активировать виртуальное окружение
.\.venv\Scripts\Activate.ps1
```

Если политика выполнения скриптов запрещает запуск Activate.ps1, разрешите запуск локальных скриптов только для текущей сессии:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process
.\.venv\Scripts\Activate.ps1
```

## 3. Установка зависимостей
```powershell
# Обновить pip и установить все зависимости проекта
python -m pip install --upgrade pip
pip install -r requirements.txt
```

## 4. Настройка переменных окружения через файл .env
Шаблон файла уже находится в корне проекта. Чтобы быстро заполнить ключевые поля, воспользуйтесь следующими командами (замените `***` на реальные значения):
```powershell
# Открыть .env в блокноте для редактирования (или используйте любой редактор)
notepad .env
```

Минимально необходимо указать:
- `TELEGRAM_BOT_TOKEN`
- `ADMIN_TELEGRAM_BOT_TOKEN` и `ADMIN_USER_IDS` (если запускаете административного бота)
- `DEEPSEEK_API_KEY` (если используете генерацию ответов через DeepSeek)
- `DATABASE_URL` для реферального сервиса, если используете PostgreSQL вместо SQLite

Альтернативно можно задать отдельные переменные только на время PowerShell-сессии:
```powershell
$Env:TELEGRAM_BOT_TOKEN = "***"
$Env:ADMIN_TELEGRAM_BOT_TOKEN = "***"
$Env:ADMIN_USER_IDS = "123456789,987654321"
$Env:DEEPSEEK_API_KEY = "***"
$Env:DATABASE_URL = "postgresql+asyncpg://user:password@localhost:5432/aura"
```

## 5. Быстрый запуск всех сервисов одной командой
После подготовки окружения и `.env` достаточно выполнить одну команду — она поднимет основной бот, административный бот и реферальный API в параллельных процессах. Логи каждого сервиса будут выводиться в консоль.

```powershell
# Запустить все сервисы проекта Aura-Ai
python run_all.py
```

Сценарий перехватывает `Ctrl+C` и корректно останавливает все дочерние процессы. По умолчанию реферальный API поднимается на `http://127.0.0.1:8000` (хост и порт можно изменить переменными `REFERRAL_API_HOST` и `REFERRAL_API_PORT`).

## 6. Ручной запуск отдельных сервисов (по необходимости)
Если нужно запустить компоненты по отдельности (например, для отладки), воспользуйтесь следующими командами:

```powershell
# Основной Telegram-бот
python Aura_Psycholog_bot.py

# Административный бот
python -m admin_bot

# Реферальный backend-сервис
uvicorn Aura_Psycholog_bot:referral_api --reload --host 0.0.0.0 --port 8000
```

Документация API будет доступна по адресу http://127.0.0.1:8000/docs.

## 7. Остановка процессов
```powershell
# Для сценария run_all.py достаточно нажать Ctrl+C в той же консоли
# Для ручного запуска прервите каждый процесс сочетанием Ctrl+C
```

## 8. Деактивация виртуального окружения
```powershell
# Вернуться в системный Python
deactivate
```

На этом запуск проекта завершён. При следующем входе в каталог достаточно активировать виртуальное окружение и воспользоваться `python run_all.py` или точечно запустить нужные сервисы:
```powershell
Set-Location Aura-Ai
.\.venv\Scripts\Activate.ps1
python run_all.py
```
