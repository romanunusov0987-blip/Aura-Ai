# Гайд по запуску Aura-Ai через PowerShell

Ниже приведена последовательность готовых команд для Windows PowerShell, которая поможет развернуть и запустить все основные компоненты проекта Aura-Ai: основного Telegram-бота, административного бота и реферальный backend-сервис.

> 💡 Предполагается, что Git, Python 3.11+ и PostgreSQL (для реферального сервиса) уже установлены и доступны в PATH.

## 1. Клонирование репозитория и переход в каталог проекта
```powershell
# Склонировать репозиторий (при необходимости замените URL на свой форк)
git clone https://github.com/your-account/Aura-Ai.git

# Перейти в папку проекта
cd C:\Users\РОМА\Desktop\Aura-Ai-main
```

## 2. Установка зависимостей в системный Python
```powershell
# Обновить pip и установить все зависимости проекта в профиль пользователя
python -m pip install --user --upgrade pip
python -m pip install --user -r requirements.txt

# (Опционально) добавить скрипты Python в PATH для текущей сессии
$userScripts = "$Env:APPDATA\Python\Python311\Scripts"
if (Test-Path $userScripts) { $Env:Path = "$userScripts;$Env:Path" }
```

## 3. Настройка переменных окружения через файл .env
Шаблон файла уже находится в корне проекта. Чтобы быстро заполнить ключевые поля, воспользуйтесь следующими командами (замените `***` на реальные значения):
```powershell
# Открыть .env в блокноте для редактирования (или используйте любой редактор)
notepad .env
```

Минимально необходимо указать:
- `TELEGRAM_BOT_TOKEN`
- `ADMIN_TELEGRAM_BOT_TOKEN` (если запускаете административного бота)
- `DEEPSEEK_API_KEY` (если используете генерацию ответов через DeepSeek)
- `DATABASE_URL` для реферального сервиса, если используете PostgreSQL

Альтернативно можно задать отдельные переменные только на время PowerShell-сессии:
```powershell
$Env:TELEGRAM_BOT_TOKEN = "***"
$Env:ADMIN_TELEGRAM_BOT_TOKEN = "***"
$Env:DEEPSEEK_API_KEY = "***"
$Env:DATABASE_URL = "postgresql+psycopg://user:password@localhost:5432/referral_db"
```

## 4. Создание таблиц реферального сервиса (опционально)
Если вы используете PostgreSQL для реферального сервиса и хотите создать таблицы заранее, выполните команду:
```powershell
python -c "import asyncio; import Aura_Psycholog_bot; from db import init_db; asyncio.run(init_db())"
```

## 5. Запуск основного Telegram-бота
```powershell
# Перед запуском заполните .env необходимыми токенами
python Aura_Psycholog_bot.py
```

Бот будет получать обновления через Telegram API (long polling). Логи выводятся в консоль.

## 6. Запуск административного бота
```powershell
# Требуется, чтобы переменные ADMIN_TELEGRAM_BOT_TOKEN и ADMIN_USER_IDS были заданы
python -m admin_bot
```

## 7. Запуск реферального backend-сервиса
```powershell
# Для авто-перезагрузки в режиме разработки используем uvicorn из текущего Python
python -m uvicorn Aura_Psycholog_bot:referral_api --reload --host 0.0.0.0 --port 8000
```

После запуска API документация будет доступна по адресу http://127.0.0.1:8000/docs

## 8. Параллельный запуск сервисов (опционально)
Если нужно держать несколько процессов одновременно, можно использовать отдельные вкладки Windows Terminal или команды PowerShell Background Jobs:
```powershell
# Пример запуска бота в фоне
Start-Job -ScriptBlock { Set-Location "$(Get-Location)"; python Aura_Psycholog_bot.py }

# Пример запуска реферального API в новой вкладке Windows Terminal
wt -w 0 nt -p "Windows PowerShell" powershell -NoExit -Command "Set-Location '$(Get-Location)'; python -m uvicorn Aura_Psycholog_bot:referral_api --reload"
```
## 9. Остановка процессов
```powershell
# Остановить фоновые джобы, если использовали Start-Job
Get-Job | Stop-Job
Get-Job | Remove-Job
```
